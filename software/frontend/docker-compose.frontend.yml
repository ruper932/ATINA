version: '3.8'

services:
  frontend-app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=http://192.168.1.80:8000/api/v1
    container_name: atina_frontend_prod
    restart: always
    ports:
      - "8080:80"
